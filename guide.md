# MindPilot 复现指南

## 1. 项目核心思想

<!-- prettier-ignore -->
给定一个从真实脑电信号中离线提取的目标神经特征向量 \(y_{\text{target}}\)，在完全不知道原始图像的情况下，迭代生成图像 \(u\)，使 \(f(g(u))\) 与 \(y_{\text{target}}\) 的余弦相似度最大化。其中 \(g\) 为图像到脑电的黑箱代理模型，\(f\) 为脑电到特征向量的编码器。整个过程仅以目标脑电特征为优化方向，不依赖文本提示或目标图像，基于扩散模型生成具有语义的自然图像。

---

## 2. 代码目录结构与功能

### 2.1 项目根目录

| 目录/文件 | 主要功能 |
| --- | --- |
| `model/` | 存放所有核心算法和模型实现：脑电特征提取、图像到脑电的映射、图像生成模型的包装、奖励预测模型等。 |
| `experiments/` | 实验入口与评估脚本：包含对比实验、离线生成、启发式生成、交互式搜索和各类工具函数。 |
| `server/` | 在线闭环实验的服务端：负责接收脑电信号、运行优化流程并返回生成的图像。 |
| `client/` | 在线闭环实验的客户端：负责在屏幕上呈现图像、采集脑电信号和记录用户反馈。 |
| `data/` | 数据缓存目录：存放图像素材、预计算的特征文件和各种模型权重。 |
| `benchmark_results_total/` | 对比实验的输出目录，包含结果表格、统计信息和生成的图像。 |

### 2.2 `experiments/` 核心文件功能

| 文件 | 实际做什么 |
| --- | --- |
| `benchmark_framework_total.py` | 总对比实验的入口程序。加载配置，运行多种图像生成方法并对比效果。 |
| `benchmark_config_total.json` | 总对比实验的配置文件（目标图像数量、搜索预算、启用哪些方法等）。 |
| `benchmark_offline_generation.py` | 离线生成对比实验。专门比较三种方式：靠脑电信号找图、靠目标图片本身找图、完全随机找图。 |
| `exp_batch_offline_generation.py` | 批量离线生成脚本，适合一次测试多个目标。 |
| `exp-benchmark_heuristic_generation.py` | 启发式生成对比实验。模拟闭环环境，每轮根据上一轮的结果调整搜索方向。 |
| `util.py` | 实验流程工具集：图像预处理、脑电信号生成、结果保存、可视化和模型构建等。 |
| `extended_diffusers.py` | 对图像生成库的扩展，补充生成流程所需的自定义能力。 |

---

## 3. 数据流

### 3.1 对比实验初始化流程

1. 主程序读取配置文件，得到目标图像数量、搜索预算等参数。
2. 配置中的路径占位符被替换成环境变量文件中的真实路径。
3. 加载所有必要的模型：脑电特征提取器、图像到脑电的编码器，必要时加载图像生成主模型。
4. 从存放图像和特征文件的目录中读取候选目标（图像文件与对应的特征文件按名称一一配对）。
5. 从所有候选中随机抽取指定数量的目标，形成本次评测的任务列表。

### 3.2 三种核心工作模式

根据论文第3.2节的设计，MindPilot 有三种递进的图像优化策略：

#### 模式一：交互式搜索（在现有图像池里找）

**场景**：我们有一个包含几百张图像的素材库，要从里面找出最能诱发目标大脑反应的那张。

**工作流程**：

1. 开始时，库中所有图像被选中的概率相等（都是 1/N）。
2. 随机选几张图像，模拟看到每张图时的大脑反应，与目标大脑反应计算相似度（这个相似度就是“奖励分数”）。
3. **直接奖励更新**：对得分最高的几张图，用奖励分数更新它们的“得分”——公式是将历史得分和新奖励按一定比例混合（类似滑动平均），这样既参考新信息又保留历史积累。
4. **奖励传播**：高得分图像的奖励还会“扩散”给素材库中与它们语义相似的其他图像。这样，“长得像高分图像”的图片也会被加分。
5. 根据更新后的得分，用概率抽样的方式选择下一轮要看的图像（得分越高，被选中的概率越大，但不是绝对确定——保留探索余地）。
6. 重复这个“选图→看反应→更新分数→奖励扩散→再选图”的循环，直到找到足够好的图像或用完预算。

**关键设计思想**：给高相似度的图像加分，相似奖励在图像间按语义相近程度扩散，然后用概率而不是贪婪选择来保持探索能力。

#### 模式二：启发式生成（生成新图像，而不是从库里找）

**场景**：不局限于已有的素材库，而是根据大脑反应反馈，直接合成全新的图像。

**工作流程**（在前述搜索的基础上）：

1. 从素材库中选出充当“父本”的图像（按前述概率抽样方式）。
2. **交叉操作**：取两张高分图像的语义特征向量，随机交换部分片段，生成一个“混合”特征。
3. **新图像生成**：将这个混合特征作为条件，输入图像生成模型，合成一张全新的图像——这张图继承了父本的语义特点，但又是全新的组合。
4. **新增素材注入**：定期从原始素材库中随机加入全新图像，防止搜索陷入死胡同。
5. 对新生成的图像也计算大脑反应相似度，更新得分和选择概率。
6. 对全局得分最高的图像进行“精英保留”，确保最优结果不会在迭代中丢失。

**关键设计思想**：交叉操作模拟“基因重组”，结合父本的优点探索新方向；新增注入保持多样性；精英保留保证质量不倒退。

#### 模式三：基于代理模型的梯度引导（论文核心创新）

**场景**：从极少量样本出发，在图像生成模型的内部空间中高效搜索。

**工作流程**：

1. 随机采样少量图像（比如 10 张），计算每张图与目标的大脑反应相似度作为奖励。
2. 用这些（图像语义特征 → 奖励）数据训练一个高斯过程代理模型。它的作用是：给定任意图像特征，快速预测对应的奖励大致是多少，而不需要真正去生成图像和计算大脑反应。
3. 从一个随机初始化的特征向量出发，利用代理模型提供的“预测梯度”进行优化，逐步找到期望奖励最高的特征位置（即“伪目标特征”）。
4. 将这个伪目标特征作为图像生成模型的条件输入，合成最终图像。

**为什么用这个策略**：

- 在高维空间中直接做传统优化（如标准贝叶斯优化）会遇到“维度灾难”，效率极低。
- 代理模型只关注当前优化位置附近的局部区域，用局部梯度来指导下一步往哪走，避免了全局搜索的巨量计算。
- 同时，代理模型能较好地处理脑电信号中的噪声和不确定性。

---

## 4. 多种搜索与生成方法的区别

论文通过在相同任务下对比多种方法，验证 MindPilot 策略的效率和稳定性。所有方法的目标相同：在图像生成模型的内部空间中，找到能引发特定大脑反应的图像。区别在于“搜索方向由谁提供、如何提供”。

| 方法 | 基本思路 | 论文中的角色 |
| --- | --- | --- |
| **伪模型离线生成** | 用少量随机采样数据训练代理模型，再用代理模型指导梯度上升，找到最优的特征位置后生成图像。 | MindPilot 的核心方法，强调样本效率：用很少的尝试就能取得不错的效果。 |
| **伪模型闭环生成** | 在上述基础上加入迭代反馈，每生成一张新图就立即评估，更新代理模型后再生成下一张。 | 体现“边观察大脑反应边修正刺激”的闭环思想。 |
| **基于策略梯度的强化学习** | 把图像生成过程看作一个策略，用强化学习算法训练它输出高奖励图像。 | 对比基线：测试训练式强化学习在高维图像空间的适用性。 |
| **带约束的强化学习** | 在策略梯度基础上加入分布约束，防止生成结果偏离正常图像太远。 | 检验另一类策略优化方法的效率和稳定性。 |
| **偏好强化学习** | 利用成对比较（A图比B图好）来训练生成模型，而不是给绝对分数。 | 补足强化学习类方法的代表性对比。 |
| **标准贝叶斯优化** | 直接在高维空间中进行传统贝叶斯优化。 | 对应论文对“维度灾难”的讨论：证明经典方法在这种场景下效率不高。 |
| **进化策略** | 基于种群进化思想的优化算法，不依赖梯度，通过变异和选择逐步优化。 | 作为另一类经典黑箱优化方法的对比。 |

---

## 5. 复现实验步骤

1. **配置环境变量文件 `mindpilot.env`**：确认项目根目录、输出目录、图像素材路径、特征文件路径、模型权重路径都正确。计算设备设置为 `cuda`。

2. **安装依赖环境**：按照项目提供的 `setup.sh` 或 `environment.yml` 创建环境，确保 PyTorch 为 CUDA 版本。

3. **下载数据和模型权重**：将 MindPilot 数据集和检查点文件放置在指定目录。

4. **运行总对比实验**：

   ```bash
   python experiments/benchmark_framework_total.py --config experiments/benchmark_config_total.json --exp exp1
   ```

5. **调整实验规模（可选）**：修改配置文件中的目标数量和采样预算后重跑。

6. **快速验证**：先用小规模配置进行冒烟测试，确认流程通畅。

### 5.1 复现清单

#### 路径 A：使用作者检查点复现

1. 下载 MindPilot 的 Hugging Face 资源包，并按 README 下载 THINGS-EEG2 外部数据集。
2. 检查 `mindpilot.env` 中的路径是否都能解析到本地目录，尤其是测试图像、`eeg_embed`、EEG 模型和 encoding model。
3. 先跑离线生成基准：`bash experiments/run_benchmark_offline_generation.sh`。
4. 再跑总 benchmark：`bash experiments/run_benchmark_total.sh`。
5. 如果只想复现主对比结果，优先检查 `experiments/benchmark_config_total.json` 中 `exp1` 和方法开关是否符合你的实验范围。

#### 路径 B：从头训练检查点复现

1. 先训练 EEG readout 模型：`python model/end_to_end.py`，这是仓库里明确提供的训练入口。
2. 将训练得到的 checkpoint 写入或替换到 `mindpilot.env` 和 benchmark 配置所指向的位置。
3. 如果需要重建 `eeg_embed` 这类离线特征缓存，先完成图像到 EEG 的生成，再按项目中已有流程导出对应特征文件。
4. 最后回到路径 A，重新跑离线生成和总 benchmark，得到完整可比的结果。

---

## 6. 输出结果位置

- **详细结果表**：`benchmark_results_total/exp1_results.csv`（每行记录一次生成实验的目标索引、方法、相似度分数等）
- **汇总表**：`benchmark_results_total/exp1_summary.csv`（按方法汇总的平均值和标准差）
- **生成图像**：`benchmark_results_total/images/...`（按方法和目标分类存放）

---

## 7. 下载模型的功能说明

| 模型标识 | 实际功能 |
| --- | --- |
| 数据集与预计算特征 | 包含测试图像、预提取的脑电特征、被试模型、图像到脑电的编码权重等。是整个项目的数据基础。 |
| 图像生成主模型（SDXL） | 根据输入的语义特征向量生成高质量自然图像。是整个生成流程的引擎。 |
| 图像条件适配器 | 让生成模型能够根据图像语义特征向量（而不是文本）来控制生成内容。它接收优化得到的特征，驱动生成过程。 |
| 图像语义特征提取模型 | 将任意图像转换为固定长度的语义向量（1024维），用于计算图像间的语义相似度，以及作为代理模型的输入。 |
| 图像编/解码器 | 在图像和压缩后的潜在表征之间互相转换，是生成模型内部必需的组件。 |
| 加速版生成模型 | 通过蒸馏技术减少生成步数，可快速产出图像。当前代码默认使用标准版，可用于后续快速实验。 |

---

## 8. 离线生成对比实验详解

如果你只想快速了解“脑电引导比随机生成强多少？距离直接用目标图像引导还有多远？”，可以运行离线生成脚本。

### 8.1 任务设定

给定 200 张测试图像作为目标，分别用三种方式生成图像，并评估生成结果与原始目标在大脑反应空间和语义空间中的相似度。

### 8.2 三种方法

| 方法 | 优化依据 | 含义 |
| --- | --- | --- |
| 脑电引导（MindPilot） | 目标图像的预存脑电特征 | 利用大脑信号间接逼近目标 |
| 目标图像引导（理论上限） | 目标图像的实时语义向量 | 直接知道目标“长什么样”，反映理论上能达到的最好效果 |
| 随机生成（零基线） | 无 | 完全不做优化，反映随机生成的表现基线 |

### 8.3 核心步骤

1. 按顺序选一张目标图像。
2. 获取对应的目标特征（脑电特征从预存文件读取，语义向量由图像特征提取模型实时计算）。
3. 从素材库中随机抽取 10 张不含目标本身的图片，计算每张图与目标的相似度作为奖励。
4. 用这 10 组（图像语义向量 → 负奖励）数据训练一个代理模型。
5. 从随机向量出发，利用代理模型做梯度上升，找到奖励最高的语义向量。
6. 将该向量作为条件输入生成模型，合成最终图像。
7. 计算生成图像与原始目标的脑电相似度和语义相似度。

---

**核心思想回顾**：MindPilot 利用少量探索获得的奖励信息，构建一个从“图像看起来像什么”到“它能引发什么大脑反应”的预测模型，然后在该模型的指引下搜索最佳图像表达，最终用生成模型合成新图像。整个过程无需文本提示，也不依赖目标图像，仅靠脑电信号完成从探索到生成的全流程。

当前启发式benchmark脚本生成的目录非常混乱,是类似这样的：
- benchmark_results_total/benchmark_heuristic_generation/
   - 时间戳/
      - eeg_guidance/
         - target_x_seed_y/
            - loop1/
               visualization_iteration_0.png
               - generated_imgs/(空)
            - loop2/
            ...
            - plots/(空)
            - final_generated_0.png
            - final_generated_1.png
            - similarities.jpg
            ...
   - 时间戳/
      - target_image_guidance/
         - target_x_seed_y/
            - loop1/
               visualization_iteration_0.png
               - generated_imgs/(空)
            - loop2/
            ...
            - plots/(空)
            - final_generated_0.png
            - final_generated_1.png
            - similarities.jpg
            ...

请你修改为：
- benchmark_results_total/benchmark_heuristic_generation/
   - eeg_guidance/
      - target_x_seed_y/
         - loop1.png
         - loop2.png
         ...
         - final/
            - final_generated_0.png
            - final_generated_1.png
            - final_generated_2.png
            - final_generated_3.png
            - final_generated_4.png
            - similarities.jpg
   - target_image_guidance/
      ...
   - random_generation/
      ...